spring:
  application:
    name: conference-service

  config:
    import: optional:configserver:http://localhost:8888/

logging:
  level:
    com.conferenchub.conferenceservice.conference.client: DEBUG
