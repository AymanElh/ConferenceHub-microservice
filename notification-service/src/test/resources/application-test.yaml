spring:
  cloud:
    config:
      enabled: false
      fail-fast: false
  config:
    import: ""
  mongodb:
    uri: mongodb://localhost:27017/notification-test
  kafka:
    bootstrap-servers: localhost:9092
  mail:
    host: localhost
    port: 1025

eureka:
  client:
    enabled: false