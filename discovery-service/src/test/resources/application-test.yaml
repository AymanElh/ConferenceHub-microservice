# Bypasses config-server during tests
spring:
  cloud:
    config:
      enabled: false
      fail-fast: false
  config:
    import: ""   # Override the configserver import

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/